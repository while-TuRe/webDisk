#include <string>
using namespace std;

string tmp_upload_table;

if (file not exist)
{
    for(i = 0; i < part_num; i++)
        tmp_upload_table.add(file / part_num)    
}

read_item_from_db

struct file
{
    string md5;
    int has_uploaded_num;
    map<user, is_uploading>user;
    vector<part>parts;
};


struct datadb_item
{
    md5 varchar(10),
        start int,
        end int;
}


struct Client
{
    int start_pos;
    int end_pos;
    int fd;
};
